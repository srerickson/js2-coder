#cloud-config
hostname: dream-machine
# package_update: true
# package_upgrade: true
packages:
  - git
users:
  - name: dreamer
    sudo: ALL=(ALL) NOPASSWD:ALL
    plain_text_passwd: funtimes
    lock_passwd: false
runcmd:
  - dnf update -y
  - dnf upgrade -y
  - sudo -u dreamer podman pull docker.io/rocker/tidyverse:latest
  - [rm, -f, /home/dreamer/.ssh/authorized_keys]
  - [cloud-init, clean, -l, -s, --machine-id, -c, all]
  - [poweroff]